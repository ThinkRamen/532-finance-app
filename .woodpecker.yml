steps:
    build:
        image: docker:dind
        commands:
            - dockerd &
            - sleep 5 # Wait for daemon to start
            - echo "Building finance app..."
            - docker compose build --no-cache
            - docker compose up -d
        when:
            event: push
            branch: main
